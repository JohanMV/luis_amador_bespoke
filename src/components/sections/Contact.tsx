import { Clock3, MapPin, MessageCircle } from "lucide-react";
import { Button, Reveal, SectionHeading } from "@/components/ui";
import { useContactForm } from "@/hooks";
import { whatsappUrl } from "@/lib/config";

export function Contact() {
  const { fields, update, submit } = useContactForm();
  const fieldClass = "h-13 w-full border-b border-ink/25 bg-transparent px-0 text-sm placeholder:text-ink-soft/35 focus:border-gold focus:outline-none";
  return (
    <section id="contacto" className="bg-[#dcd3c4] py-20 sm:py-28 lg:py-36"><div className="section-shell"><div className="grid overflow-hidden bg-ivory-light shadow-editorial lg:grid-cols-2"><Reveal className="p-6 sm:p-10 lg:p-14"><SectionHeading eyebrow="Atención personal" title="Agenda tu cita" description="Cuéntanos qué estás buscando. Te responderemos por WhatsApp para confirmar el horario y preparar tu visita." /><form onSubmit={submit} className="mt-9 grid gap-x-6 sm:grid-cols-2"><label className="text-[0.64rem] font-semibold uppercase tracking-widest">Nombre completo<input required value={fields.name} onChange={(event) => update("name", event.target.value)} className={fieldClass} placeholder="Tu nombre" /></label><label className="mt-6 text-[0.64rem] font-semibold uppercase tracking-widest sm:mt-0">Celular<input required inputMode="tel" value={fields.phone} onChange={(event) => update("phone", event.target.value)} className={fieldClass} placeholder="+51 999 999 999" /></label><label className="mt-6 text-[0.64rem] font-semibold uppercase tracking-widest sm:col-span-2">Me interesa<select value={fields.occasion} onChange={(event) => update("occasion", event.target.value)} className={fieldClass}><option>Terno a medida</option><option>Alquiler de terno</option><option>Vestido de novia</option><option>Vestido de ceremonia</option></select></label><label className="mt-6 text-[0.64rem] font-semibold uppercase tracking-widest sm:col-span-2">Cuéntanos un poco<textarea value={fields.message} onChange={(event) => update("message", event.target.value)} rows={3} className="mt-3 w-full resize-none border-b border-ink/25 bg-transparent py-3 text-sm placeholder:text-ink-soft/35 focus:border-gold focus:outline-none" placeholder="Fecha del evento, estilo que buscas..." /></label><Button type="submit" variant="gold" className="mt-8 sm:col-span-2">Solicitar cita por WhatsApp</Button></form></Reveal><div className="relative min-h-[500px] bg-ink"><iframe title="Mapa de LuisAmador en Centro de Lima" src="https://www.google.com/maps?q=Jr.%20Huallaga%20305%2C%20Lima%2015001&output=embed" className="absolute inset-0 h-full w-full border-0 grayscale contrast-[.9]" loading="lazy" /><div className="absolute inset-x-4 bottom-4 bg-ink/95 p-6 text-white backdrop-blur sm:inset-x-8 sm:bottom-8"><div className="grid gap-5 sm:grid-cols-2"><div className="flex gap-3"><MapPin size={18} className="shrink-0 text-gold" /><p className="text-xs leading-5 text-white/70">Jr. Huallaga 305<br />Centro de Lima</p></div><div className="flex gap-3"><Clock3 size={18} className="shrink-0 text-gold" /><p className="text-xs leading-5 text-white/70">Lun–Sáb · 10:00–20:00<br />Domingo con cita</p></div></div><a href={whatsappUrl("Hola, deseo visitar el atelier de LuisAmador.")} target="_blank" rel="noreferrer" className="mt-5 flex items-center gap-2 border-t border-white/10 pt-5 text-xs uppercase tracking-widest text-gold-light"><MessageCircle size={15} /> +51 916 842 610</a></div></div></div></div>
    </section>
  );
}
